#OTC react application
This application was built to manage several OTC engineering and operations tools.

##Installation

from the console, 
    run `npm install`
    run `npm start`

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).